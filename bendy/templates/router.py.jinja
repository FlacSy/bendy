from uuid import UUID

from fastapi import APIRouter, Depends, status
from sqlalchemy.ext.asyncio import AsyncSession

from shared.database import get_session
from ..application.dtos import (
    {{ domain_name|capitalize }}Create,
{%- if 'update' in use_cases %}
    {{ domain_name|capitalize }}Update,
{%- endif %}
    {{ domain_name|capitalize }}Response,
)
from ..application.use_cases import (
{%- if 'create' in use_cases %}
    Create{{ domain_name|capitalize }}UseCase,
{%- endif %}
{%- if 'get' in use_cases %}
    Get{{ domain_name|capitalize }}UseCase,
{%- endif %}
{%- if 'update' in use_cases %}
    Update{{ domain_name|capitalize }}UseCase,
{%- endif %}
{%- if 'delete' in use_cases %}
    Delete{{ domain_name|capitalize }}UseCase,
{%- endif %}
{%- if 'list' in use_cases %}
    List{{ domain_name|capitalize }}sUseCase,
{%- endif %}
)
from ..infrastructure.uow import {{ domain_name|capitalize }}UnitOfWork

router = APIRouter(prefix="/{{ domain_name }}s", tags=["{{ domain_name|capitalize }}"])


def _uow(session: AsyncSession = Depends(get_session)) -> {{ domain_name|capitalize }}UnitOfWork:
    return {{ domain_name|capitalize }}UnitOfWork(session)

{%- if 'list' in use_cases %}


@router.get("/", response_model=list[{{ domain_name|capitalize }}Response])
async def list_{{ domain_name }}s(uow=Depends(_uow)):
    return await List{{ domain_name|capitalize }}sUseCase(uow).execute()
{%- endif %}
{%- if 'create' in use_cases %}


@router.post("/", response_model={{ domain_name|capitalize }}Response, status_code=status.HTTP_201_CREATED)
async def create_{{ domain_name }}(data: {{ domain_name|capitalize }}Create, uow=Depends(_uow)):
    return await Create{{ domain_name|capitalize }}UseCase(uow).execute(data)
{%- endif %}
{%- if 'get' in use_cases %}


@router.get("/{id}", response_model={{ domain_name|capitalize }}Response)
async def get_{{ domain_name }}(id: UUID, uow=Depends(_uow)):
    return await Get{{ domain_name|capitalize }}UseCase(uow).execute(id)
{%- endif %}
{%- if 'update' in use_cases %}


@router.patch("/{id}", response_model={{ domain_name|capitalize }}Response)
async def update_{{ domain_name }}(id: UUID, data: {{ domain_name|capitalize }}Update, uow=Depends(_uow)):
    return await Update{{ domain_name|capitalize }}UseCase(uow).execute(id, data)
{%- endif %}
{%- if 'delete' in use_cases %}


@router.delete("/{id}", status_code=status.HTTP_204_NO_CONTENT)
async def delete_{{ domain_name }}(id: UUID, uow=Depends(_uow)):
    await Delete{{ domain_name|capitalize }}UseCase(uow).execute(id)
{%- endif %}
